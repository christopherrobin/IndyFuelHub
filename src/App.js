import React, { Component } from 'react';
import RosterList from './RosterList';
import './App.css'

class App extends Component {
  render() {
    return (
      <div className="App">
        <RosterList/>
      </div>
    );
  }
}

export default(App);
